{
  "name": "capture",
  "version": "1.0.0",
  "private": true,
  "description": "Capture — personal dictation notes app. Record, transcribe via Deepgram, save and edit notes. Cloudflare Pages + PWA.",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy --branch production"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
